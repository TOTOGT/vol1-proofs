
## AutophagyMerged_draft.lean, AutophagyMerged_selfcheck.lean

Superseded 2026-08-25. These are the two divergent on-disk copies that
`AutophagyDm3_v2.lean` is the merge of — see the lakefile's note on that
target. They are not built, are not in `defaultTargets`, and contribute no
theorems to `N`. They are kept here rather than deleted because the merge is
part of the record, and moved off the top level because two unbuilt copies of
a file beside the built one is the exact shape that produced the V6 drift.
